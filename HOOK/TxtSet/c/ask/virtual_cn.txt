
B b;
typedef void (*Fun)();

Fun pf = (Fun)*(int *)*(int *)&b;

虚函数的对象的首个地址处(&b)，就是虚函数指针
所以将首个地址转换为指向四字节数组的指针：(int*)(&b)。
取第一个*内容(int*)(&b)[0]或*(int*)(&b)

*(int*)(&b)也是一个指针，指向函数，所以把这个内容转换为指针(int *)(int *)*(int *)&b

*(int *)(int *)*(int *)&b取内容，就是一个函数指针，强制类型转成目标函数(Fun)*(int *)*(int *)&b