void  SpaceAndTabNum(string str,int& sNum,int &tNum)
{
	int p=0;
	sNum = 0;//space count
	tNum = 0;//tab count
	while (' ' == str[p] || '	' == str[p])
	{
		    
			if (' ' == str[p])
			sNum++;
		if ('	' == str[p])
			tNum++;
		p++;
	}
}